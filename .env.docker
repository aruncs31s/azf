# Docker environment configuration
PORT=8080
ENVIRONMENT=production

# JWT Configuration
JWT_SECRET=docker-secret-change-in-production

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Casbin Configuration
CASBIN_MODEL_FILE=/app/config/casbin_rbac_model.conf
CASBIN_POLICY_FILE=/app/config/casbin_rbac_policy.csv

# Database Configuration (SQLite in container)
DB_USER=root
DB_PASS=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=/app/data/authorization.db

# Logging
LOG_LEVEL=info
